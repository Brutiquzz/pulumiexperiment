name: ${PROJECT}
description: ${DESCRIPTION}
runtime: dotnet
template:
  description: My pulumi template test
  config:
    region:
      description: The Azure region to deploy to
      default: west europe
packages:
  pulumicomponents: git://github.com/Brutiquzz/pulumicomponents@0.0.4
